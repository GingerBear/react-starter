import React, { Component } from 'react';
import './Header.css';

class NoMatch extends Component {
  render() {
    return (
      <header>fullsite demo</header>
    );
  }
}

export default NoMatch;
